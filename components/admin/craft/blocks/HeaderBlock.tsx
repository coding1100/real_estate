"use client";

import { BlockCard } from "./BlockCard";

export function HeaderBlock() {
  return (
    <BlockCard
      label="Header"
      description="Brand bar at the top of the page."
    />
  );
}

