"use client";

import { BlockCard } from "./BlockCard";

export function HeroBadgeStripBlock() {
  return (
    <BlockCard
      label="Hero badges"
      description="Optional badges or small highlights row."
    />
  );
}

