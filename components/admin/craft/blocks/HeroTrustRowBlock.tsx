"use client";

import { BlockCard } from "./BlockCard";

export function HeroTrustRowBlock() {
  return (
    <BlockCard
      label="Hero trust row"
      description="Logos or small text row under the hero for social proof."
    />
  );
}

